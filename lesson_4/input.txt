В текстовом( файле input.txt в записаны строки),
в которых есть скобки. Удалить (в каждой строке ( текст в (одинарных) )скобках и эти скобки. )
Если есть  (вложенность, )текст (во вложенных )скобках ( не) изменять.
(Использовать  String,  StringBuffer и StringTokenizer.)
В текстовом( файле input.txt в записаны строки),
в которых есть скобки. Удалить (в каждой строке ( текст в (одинарных) )скобках и эти скобки. )
Если есть  (вложенность, )текст (во вложенных )скобках ( не) изменять.
(Использовать  String,  StringBuffer и StringTokenizer.)